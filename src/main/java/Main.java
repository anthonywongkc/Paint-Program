import service.PaintApp;

public class Main {
    /**
     * Application entry point.
     * @param args application command line arguments
     **/
    public static void main(String[] args) {
            PaintApp app = new PaintApp();
            app.run(args);
    }
}
